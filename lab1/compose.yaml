services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3001:3001'
    volumes:
      - .:/src
      - node_modules:/src/node_modules
    environment:
      NODE_ENV: development

volumes:
  node_modules: {}
